# Copy to .env.local:  cp .env.example .env.local
# Do not commit .env.local.

# Neon — use the pooled URL for the app (host contains `-pooler`).
DATABASE_URL="postgresql://USER:PASSWORD@YOUR-NEON-HOST-pooler.REGION.aws.neon.tech/neondb?sslmode=require"

# Direct URL for `prisma migrate` (local / CI). Also: DATABASE_URL_UNPOOLED, POSTGRES_URL_NON_POOLING, DATABASE_DIRECT_URL.
# If omitted, `prisma.config.ts` derives a direct Neon URL from DATABASE_URL when the host contains `-pooler`.
DIRECT_URL="postgresql://USER:PASSWORD@YOUR-NEON-HOST.REGION.aws.neon.tech/neondb?sslmode=require"

# Migrations are not part of `npm run build` (avoids P1002 advisory-lock timeouts on Vercel). After pulling schema changes run:
#   cd web && npm run migrate:deploy

OPENAI_API_KEY="sk-replace-with-your-real-openai-api-key"
